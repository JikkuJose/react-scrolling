import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { ReactScrolling } from 'react-scrolling';

ReactDOM.render(
	<ReactScrolling>
		<div>Hello, React Scrolling!</div>
	</ReactScrolling>,
	document.getElementById('ReactScrolling')
);
